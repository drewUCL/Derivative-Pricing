#ifndef _ _FNS_HPP // Fns.hpp
#define _ _FNS_HPP
#include <iostream>

template <class Numeric>
Numeric CND(const Numeric& d); //using the class numeric and the object Cumilative Normal Distribution algorithim. the variable d is being passed as a constant of the type numeric. this unary operator (&) means the address of within the member function
#endif